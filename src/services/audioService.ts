// Web Audio API feedback synthesizer - sounds disabled per user preference

class SoundService {
  public setEnabled(_val: boolean) {
    // sounds disabled
  }

  public playIncome() {
    // sound disabled per user preference
  }

  public playExpense() {
    // sound disabled per user preference
  }

  public playBalanced() {
    // sound disabled per user preference
  }

  public playWarning() {
    // sound disabled per user preference
  }

  public playClick() {
    // sound disabled per user preference
  }
}

export const sound = new SoundService();

